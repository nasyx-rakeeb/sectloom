{
  "name": "@sectloom/contracts",
  "private": true,
  "type": "module",
  "version": "0.2.0",
  "description": "Contracts and schemas for the Sectloom Registry",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "lint": "eslint .",
    "typecheck": "tsc --noEmit",
    "validate-fixtures": "node scripts/validate.mjs",
    "test": "pnpm run build && pnpm run validate-fixtures"
  },
  "dependencies": {
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}
