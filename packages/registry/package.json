{
  "name": "@sectloom/registry",
  "private": true,
  "type": "module",
  "version": "0.1.0",
  "description": "Deterministic registry builder and generated artifacts",
  "scripts": {
    "build": "tsx src/build.ts",
    "serve": "tsx src/serve.ts",
    "typecheck": "tsc --noEmit",
    "lint": "eslint .",
    "test": "tsx --test test/*.test.ts"
  },
  "dependencies": {
    "@sectloom/contracts": "workspace:*",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "prettier": "^3.0.0",
    "tsx": "^4.9.3",
    "typescript": "^5.4.5"
  }
}
